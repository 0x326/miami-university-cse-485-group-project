# mini-project-5

## Usage

To start:

```bash
git submodule update --init --recursive
cd geode-docker/composer/
docker-compose up -d
```

To stop:

```bash
cd geode-docker/composer/
docker-compose down
```
